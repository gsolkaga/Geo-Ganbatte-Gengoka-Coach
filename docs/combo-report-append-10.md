# 掛け合わせて正解に届くか — Standard append 10（AI 未使用、消費 0）

**対象は追加した 10 問**（`gsolkaga__standard-append-10`。韓国・イギリス・コロンビア・
シンガポール・メキシコ・ニュージーランド・カナダ・ラトビア・チリ・ルワンダ）である。
最初の 10 問の結果は [`combo-report.md`](combo-report.md) にある。

## 辞書を 1 語も足していない

この 10 問は**辞書を作り終えたあとに作った。** 辞書に合わせて出題を選んでいないし、
出題に合わせて辞書を足してもいない。指紋が一致することで確かめている。

```
用語 262 語（絞り込みに使える 180 語）
sha256: 909441b29d1483201aacffc69f1b6ddb1e8c49b8cf9335899077c0ae7de69554
```

> **自分が作った問題で測ると、作ったとおりの結果が出る。**

到達は **6 / 10 → 5 / 10** だった。1 問ぶん落ちている。
`combo-report.md` を上書きすると比べられなくなるので、別のファイルにしてある。

生成: 2026-08-19T09:33:34.169Z　`node tools/combo-report.mjs`

## 測る対象を間違えていた

`npm run coverage`（欄ごとの被覆）は「入力しても反応が無い欄」を潰す指標であり、
**「国に届くか」の指標ではない。**

キリル文字の言語固有字を足したとき `script` 単独で 11 カ国が 1 カ国になった。
しかし**そんな字が読めるのは幸運である。** 実戦は弱い手がかりを掛けて絞る。

> **1 つの欄で決まることを期待する設計は、決まらなかったときに何も言えない。**

## 出題ごとの到達点

正解タグの観察を、**毎回いちばん強く縮む欄から**貪欲に掛けた結果である。
正解を落とす欄は選んでいない。

| 問 | 正解 | 掛け合わせに使える欄 | 到達 | 経路 |
|---|---|---|---|---|
| q-kr-01 | KR | 3 | **1 カ国** | `script`(1)→1 |
| q-gb-01 | GB | 3 | **5 カ国** | `vehicle`(6)→6 `traffic_side`(24)→5 |
| q-co-01 | CO | 2 | **67 カ国** | `traffic_side`(78)→78 `script`(91)→67 |
| q-sg-01 | SG | 2 | **24 カ国** | `traffic_side`(24)→24 |
| q-mx-01 | MX | 2 | **1 カ国** | `pole`(1)→1 |
| q-nz-01 | NZ | 5 | **1 カ国** | `bollard`(1)→1 |
| q-ca-01 | CA | 3 | **28 カ国** | `road_marking`(37)→37 `traffic_side`(78)→31 `script`(91)→28 |
| q-lv-01 | LV | 4 | **1 カ国** | `sign`(1)→1 |
| q-cl-01 | CL | 1 | **46 カ国** | `season`(46)→46 |
| q-rw-01 | RW | 5 | **1 カ国** | `vehicle`(6)→6 `traffic_side`(78)→1 |

`欄(単独の国数)→掛けた後の国数` の順に読む。

## まとめ

| 到達 | 件数 |
|---|---|
| **1 カ国まで届いた** | 5 / 10 |
| 複数カ国で止まった | 5 |
| 算出不能（使える用語が無い） | 0 |

### 止まった出題と残った候補

- `q-gb-01`（正解 GB）→ 5 カ国 [GB HK KE LK UG]
- `q-co-01`（正解 CO）→ 67 カ国 [AD AE AL AR AT BA BE BO BR CA CH CL CO CR CZ DE DK DO EC EE ES FI FR GH GT HR HU IL IS IT KH KR KZ LB LT LU LV ME MG MX NG NL NO PA PE PH PL PR PT PY RE RO RS RW SE SI SK SN ST TN TR TW US UY UZ VN XK]
- `q-sg-01`（正解 SG）→ 24 カ国 [AU BD BT BW CY GB HK ID IE IN JP KE LK LS MT MY NA NP NZ SG SZ TH UG ZA]
- `q-ca-01`（正解 CA）→ 28 カ国 [AL AR AT BO BR CA CL CO CR DO EC FI GT KH KR LB MX NO PA PE PH PR PY RW TR TW US VN]
- `q-cl-01`（正解 CL）→ 46 カ国 [AE AR AU BD BO BR BW CL CO CR DO EC GH GT HK ID IN KE KH LK LS MG MX MY NA NG NZ OM PA PE PH PR PY RE RW SG SN ST SZ TH TW UG US UY VN ZA]

**残った候補を割る軸が辞書に無い、または正解タグに記録されていない。**
どちらなのかは正解タグの記述を読めば分かる。
- 記述があるのに用語が無い → **辞書に足す**
- 記述が無い → **その地点でその特徴が見えていない。足しても意味がない**

## 絞り込みに使えないが、説明はできる手がかり

`exhaustive: false` の用語は積集合に入れない。ユーカリを入れれば
ポルトガル・スペイン・ブラジルを誤って消してしまう。

**しかし完全に捨てるのも誤りだった。** オーストラリアの出題では
学習者が「ユーカリの木だらけ」と書き、`ref_flora_eucalyptus`（該当国 AU）が
割り当てられているのに、応答のどこにも現れていなかった。

> **絞り込みに使えないことと、言うべきことが無いことは別である。**

`buildNonExhaustiveHints` で別枠として渡す。件数は書かない。
**件数を書くと絞り込み力に見える。**

- `q-lv-01`（正解 LV）
    - `vehicle` EU 式の青い縦帯（左端） → よく見られる国 [AT BE BG CY CZ DE DK EE ES FI FR GR HR HU IE IT LT LU LV MT NL PL PT RO SE SI SK TR]
- `q-rw-01`（正解 RW）
    - `ground` 赤い土 → よく見られる国 [BR LK UG ZA]
    - `terrain_vegetation` ヤシの木＋濃い緑＋土が見える → よく見られる国 [LK TH]

## 足しても縮まないものを、足りないと呼んではならない

この表を最初に出したとき「pavement は 10 問すべてが AI生成。
出典から埋める必要がある」と結論した。**それは誤りだった。**

```
ai_pavement_01「アスファルト舗装」   102 カ国
ai_vehicle_01「白い横長のプレート」  101 カ国
```

出典から埋めても縮まない。**アスファルトはどこにでもある。**
観察が事実であることと、その観察が国を示すことは別である。

辞書が扱う国は 102 件。該当国が 51 件を超える用語しか無い欄は、
**辞書の欠落ではなく観察の性質**として分類する。
`ground` と `season` を埋めないと決めたのと同じ理由である。

### 観察が国を示していない欄（辞書を足しても縮まない）

| 欄 | 件数 |
|---|---|
| `pavement` | 10 |
| `vehicle` | 5 |
| `pole` | 5 |
| `sign` | 2 |
| `road_marking` | 1 |
| `traffic_side` | 1 |
| `camera` | 1 |

### 辞書で直せる欄（該当国が狭い用語しか無い）

**ここだけが出典から埋める対象である。**

- `q-cl-01` `road_marking`（不一致あり / 最も広い用語 1 カ国）
- `q-rw-01` `ground`（網羅でない / 最も広い用語 4 カ国）
- `q-rw-01` `terrain_vegetation`（網羅でない / 最も広い用語 2 カ国）

## 記述があるのに用語が 1 つも付かなかった欄

**ここが本当の辞書の欠落である。** 上の表は用語が付いた欄しか見ていないため、
これを出さないと「辞書は足りている」という誤った読みになる。

**ただし記述を読まずに埋めてはならない。** 「Gen4」や
「木の陰が北側に出ている」に用語を作っても国は縮まない。

合計 32 件。

### `architecture`（9 件）

- `q-kr-01` [easy] 工場の屋根が金属
- `q-gb-01` [easy] レンガ作りの建物、コンクリートに斜めの屋根、コンクリートに平の屋根
- `q-co-01` [easy] コンクリート製の平らな屋根、トタン板のような屋根
- `q-sg-01` [easy] コンクリート製の高層マンションが多い
- `q-mx-01` [easy] コンクリート製の平らな屋根の家が多い
- `q-nz-01` [easy] コンクリート製の平らな屋根
- `q-ca-01` [easy] レンガ作りの三角屋根
- `q-lv-01` [easy] レンガ作りの三角屋根
- `q-rw-01` [easy] コンクリート製に橙色の瓦、なだらかな屋根

### `terrain_vegetation`（6 件）

- `q-kr-01` [easy] 桜の木がある、川沿いの道
- `q-gb-01` [easy] 街路樹が整備されている、広葉樹
- `q-co-01` [easy] 街路樹がキツめのピンク色の桜
- `q-sg-01` [easy] 街路樹が整備されている
- `q-nz-01` [easy] 平らで芝生が綺麗に整備されている
- `q-cl-01` [easy] 砂漠、植物は僅か、遠くに三角の砂山が多く見える

### `sign`（6 件）

- `q-co-01` [hard] 停止看板の裏が十字架、コロンビアクロス
- `q-sg-01` [hard] 四角い白地の看板に三角や丸などの注意標識が描いてある
- `q-mx-01` [easy] 縦長長方形の四角い白い標識に赤い丸にEの文字が禁止となっている
- `q-nz-01` [easy] 逆三角形に赤枠に白地で赤い文字で「GIVE WAY」、通りをの名前が青字の横長末尾が「St」
- `q-ca-01` [easy] 速度表示に「MAXIMUM」
- `q-cl-01` [blind] 標識の裏側のパイプ部分にくぼみがある

### `pole`（2 件）

- `q-gb-01` [easy] 街路灯の根本が少しだけ黒く塗ってある、該当のポールが白黒縞塗装、
- `q-sg-01` [easy] 街路灯がコンクリート製、電柱や電線が見当たらない

### `bollard`（2 件）

- `q-sg-01` [hard] 黄色と黒の縞模様のポール
- `q-cl-01` [easy] 四角い鉄パイプ状で反射板が無い

### `ground`（2 件）

- `q-sg-01` [easy] きれいな芝生で整備されている
- `q-cl-01` [easy] 赤茶けた砂岩で覆われている、路肩は赤茶けた砂

### `season`（1 件）

- `q-kr-01` [easy] サクラが咲いているから春

### `traffic_side`（1 件）

- `q-mx-01` [hard] 一方通行、左ハンドル車から右側通行が予測可能

### `road_marking`（1 件）

- `q-mx-01` [easy] 線は無いが歩道路肩が黄色く塗装してある

### `script`（1 件）

- `q-lv-01` [easy] 小文字のiの上の点が横ニョロ線になっている

### `other`（1 件）

- `q-rw-01` [hard] ゴミがまったく落ちていない

## この指標の使い方

**欄ごとの被覆ではなく、この到達点を見て辞書を足す。**
被覆を上げても、掛け合わせて届かなければ学習者は国に辿り着けない。

> **弱い手がかりを掛けて絞るのが実戦である。**
> **1 つで決まる手がかりが見えるのは幸運であって、設計の前提にできない。**
