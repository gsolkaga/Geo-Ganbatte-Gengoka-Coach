# ボラード矛盾 13 件の実物確認ワークシート

`docs/bollard-axes-result.md` は `analyze-bollard-axes.mjs` が**再生成するため上書きされる。**
判断はこのファイルに書く。8/17 に Kiro が `data/glossary-human-axes.json` と
`data/glossary-human.json` へ反映する。

## 前提：AI はここで人間を反証できていない

**4 モデル全員が一致して人間を否定した項目は 1 件もなかった。**

| 票の状態 | 件数 |
|---|---|
| 4 モデル一致で人間と違う | **0** |
| 3 モデル一致 | 1 |
| 2〜3 モデルで割れている | 5 |
| **1 モデルしか答えていない** | **6** |

**ルーマニアの件が意味を持ったのは 4/4 一致だったからである。** ここは揃っていない。

したがってこの確認の目的は「AI に言われたから直す」ではない。
**自分の記述をより正確にする機会として使う。**

## 優先順位

**上から 3 行だけ見れば足りる。** 下 10 行は 1〜2 モデルの単独発言で、信号として弱い。

---

## 優先度 高

### 1. IS アイスランド `body_color`

**唯一 3 モデルが答えて一致した項目。** ここだけは確認する価値がある。

```
人間 : yellow   （棒ぜんぶが黄色い）
AI   : white×3  （1 モデルは unknown）
```

| 項目 | 記入 |
|---|---|
| 実物はどちらか（`yellow` / `white` / 両方ある / その他） |  |
| 両方あるなら、どちらが幹線で優勢か |  |
| **本体と反射板を取り違えていないか**（AI が反射板だけ見ている可能性） |  |
| 判断（人間の記述を維持 / 訂正 / 条件を追記） |  |

### 2. PL ポーランド `reflector_shape`

**4 モデル全部が答えた唯一の項目**（ただし割れている）。

```
人間 : diagonal_stripe   （反射板のところが斜めに赤い）
AI   : rectangle×3  triangle×1
```

| 項目 | 記入 |
|---|---|
| 反射板の形（長方形 / 斜めの帯 / 三角 / その他） |  |
| 「斜め」は形状のことか、貼り方のことか |  |
| 判断 |  |

### 3. VN ベトナム `cross_section`

```
人間 : square        （赤白の四角いコンクリの杭）
AI   : round×1  rectangular×1
```

| 項目 | 記入 |
|---|---|
| 断面（正方形 / 長方形 / 丸 / その他） |  |
| **KH カンボジアと形が違うか**（人間は 2 カ国を同じ用語にしている） |  |
| 判断 |  |

---

## 優先度 低（1〜2 モデルの単独発言。信号として弱い）

**空欄のままでよい。** 実物を見る機会があれば書く。

| 国 | 軸 | 人間の主張 | AI（票） | 実物 | 判断 |
|---|---|---|---|---|---|
| DK デンマーク | 反射板の色 | `yellow` | `orange×1 white×1` |  |  |
| DK デンマーク | 本体の塗り分け | `single_band` | `solid×2` |  |  |
| BE ベルギー | 反射板の色 | `yellow` | `red×2 white×1` |  |  |
| ES スペイン | 反射板の色 | `yellow` | `orange×1 white×1 red×1` |  |  |
| ES スペイン | 反射板の周囲 | `black` | `none×1` |  |  |
| EE エストニア | 反射板の色 | `yellow` | `white×1` |  |  |
| LV ラトビア | 反射板の色 | `yellow` | `white×1` |  |  |
| FR フランス | 太さ | `thick` | `thin×1` |  |  |
| KH カンボジア | 断面 | `square` | `round×1` |  |  |
| NZ ニュージーランド | 裏面 | `same_as_front` | `no_marking×1` |  |  |

**ES スペインの反射板の色は 3 モデルが 3 通り**（`orange` / `white` / `red`）を答えている。
**これは人間の誤りの証拠ではなく、AI に一貫した知識がないことの証拠である。**

---

## 判定不能だった 2 件（AI が `unknown`）

**食い違いではない。** AI が答えなかっただけであり、人間の主張は否定されていない。

| 国 | 軸 | 人間の主張 |
|---|---|---|
| EE エストニア | 反射板の周囲の色 | `black` |
| LV ラトビア | 反射板の周囲の色 | `black` |

---

## 追加の宿題：トルコの形状分解

`docs/glossary-human-worksheet.md` の「宿題」に記入欄がある。**そちらに書く。**

---

## 気づいたことがあれば

表に収まらない発見があればここに書く。**軸そのものの追加でもよい。**

arm1 で AI が提案して人間が挙げていなかった軸（**AI 側の得点として記録済み**）。

```
2 色の塗り分け          Qwen と Kimi が提案。人間側の gaps だった
基部の構造              埋設 / ベースプレート / 台座。3 モデルが提案
テーパー・段付き        径の変化
表面の溝・リブ
可動機構                スプリング / ヒンジ
高さ
```

**これらが実際に弁別に効くかは未検証である。** 効くと思うものがあれば書く。

```


```
