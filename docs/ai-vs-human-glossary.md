# AI が生成した用語辞書と人間が書いた用語辞書の比較

`bollard`（車道脇の短い杭・ポール）スロットについて、AI 生成と人手記述を比較した記録。

実施日 2026-08-07。AI 側は 4 モデル × 13 スロットで 517 用語を生成した中の `bollard` 分。人間側は GeoGuessr プレイヤー（最高到達 Master 4）の口述による。

## 結果

| | 最強メタ（1 カ国） | 強メタ（2 カ国・近接） | 良メタ（3〜4 カ国・近接） | 補助メタ |
|---|---|---|---|---|
| **人間が書いた 10 用語** | **8 件** | 1 件 | 1 件 | **0 件** |
| **AI が生成した 17 用語**（`gpt-oss-120b`） | **0 件** | 0 件 | 0 件 | **17 件（2〜94 カ国）** |

他の 3 モデルは `bollard` について 0 件を返した（プロンプトの抑制指示に従ったため。後述）。

## 人間が書いた用語

| 判定 | 該当国 | 記述 |
|---|---|---|
| 最強メタ | `IS` | 棒ぜんぶが黄色い |
| 最強メタ | `DK` | 白い棒に黄色い反射板、横に帯がある |
| 最強メタ | `BE` | 白い棒に黄色い反射板だけ（帯なし） |
| 良メタ | `ES` `EE` `LV` | 黄色の周りが黒くなっている |
| 最強メタ | `FR` | 赤と白で、太くて丸い棒 |
| 強メタ | `VN` `KH` | 赤白の四角いコンクリの杭 |
| 最強メタ | `BD` | 赤と白のしましま |
| 最強メタ | `PL` | 反射板のところが斜めに赤い |
| 最強メタ | `NZ` | 裏側まで赤い |
| 最強メタ | `TR` | キングジムのバインダーみたいな形 |

## AI が生成した用語（抜粋）

| 判定 | 該当国数 | 記述 |
|---|---|---|
| 補助メタ | 94 | 道路の横にある、たいてい 0.5〜1.2 m 程度の短い金属またはプラスチックの杭 |
| 補助メタ | 40 | 棒が丸い形をしている |
| 補助メタ | 7 | 棒が四角い形をしている |
| 補助メタ | 2 | 棒が三角形に細くなっている |
| 補助メタ | 38 | 光る棒がオレンジ色 |
| 補助メタ | 38 | 光る棒が赤い |

別のモデルの出力例。

```
gemma-4-31B-it : 黄色塗装のボラード → 8 カ国（NL BE GB IE AU NZ US CA）
Qwen3.6-35B    : 黄色主調の反射ポール → 2 カ国（GB IE）
```

**同じ「黄色いボラード」で該当国が 8 対 2。モデル間で矛盾している。**

## 原因：分類の軸が違う

**AI は属性を 1 つの軸で括った。人間は属性の組み合わせで分けた。**

| | 分類の軸 |
|---|---|
| AI | 形（丸／四角／三角）、色（オレンジ／赤／黄） — **単一属性** |
| 人間 | 本体の色 ＋ 反射板の色 ＋ 帯の有無 ＋ 断面形状 ＋ **裏面** — **属性の組み合わせ** |

**「黄色いボラード」という単位は存在しない。**

黄色がどこにあるかで国が分かれる。

```
本体全体が黄色                    → IS
本体が白、反射板が黄色、帯あり     → DK
本体が白、反射板が黄色、帯なし     → BE
黄色の周囲に黒                    → ES EE LV
```

AI は「黄色」で括った時点で、**識別に必要な情報を捨てていた。**

同様に「赤白」も形状で分かれる。

```
太くて丸い棒          → FR
四角いコンクリート     → VN KH
ストライプ            → BD
反射板が斜めに赤い     → PL
裏面まで赤い          → NZ
トルコ固有の形状      → TR
```

AI は「赤白」を 1 つの用語として 19 カ国に割り当てた。実際には 6 種類の別の手がかりだった。

## 役割分担への帰結

**これは AI 生成が無価値という結論ではない。両方に用途がある。**

| 担い手 | 作れる用語 | 用途 |
|---|---|---|
| **AI** | 粗い用語（40〜94 カ国） | **「絞り込めていない」ことを教える** |
| **人間** | 細かい用語（1〜3 カ国） | **実際に絞り込む** |

「道路の横にある短い金属の杭」94 カ国は辞書として必要である。学習者が「ボラードがあった」と書いたときに「それでは 94 カ国に該当します」と返すために使う。

**AI は下限を作り、人間は上限を作る。**

したがって用語辞書は 2 系統で管理する。

| ファイル | 由来 | 用途 |
|---|---|---|
| `docs/generated-glossary/` | AI 生成（原文保存） | 粗い用語の供給源 |
| `data/glossary-human.json` | 人手記述 | 実際の絞り込み |

**由来を混ぜない。** 記事で「AI が生成したもの」と「人間が足したもの」を区別できるようにするため。

## 0 件を返した 3 モデルについて

システムプロンプトの「確信のない項目は挙げない。数を埋めるために推測で作らない」に従い、3 モデルが `bollard` で 0 件を返した。

この指示を削除して再実行すると 5〜10 件を出した（`docs/generated-glossary-variant/`）。**沈黙は知識の不在ではなく指示への遵守だった。**

ただし出てきた内容は上記の通り粒度が粗く、モデル間で矛盾していた。

**「出た」は「正しい」を意味しない。** 抑制指示は品質を守る側に働いていた。

なお同じ変更で `gpt-oss-120b` は 17 件から 6 件に**減った**。最低件数の明示（「最低 5 件を挙げてください」）がアンカーとして働いた可能性がある。変更を 2 点入れたため交絡している。
