# 覚える価値のある組み合わせ

生成: 2026-08-17T02:10:02.971Z　
`scripts/extract-combos.mjs` が `data/glossary.json` から計算した。**AI を使っていない。**

## なぜこれを出すのか

習熟段階には**中級の壁**がある。

| 段階 | やっていること |
|---|---|
| 初級 | 個々のメタを覚える |
| **中級** | **組み合わせを覚える。組み合わせ爆発するため最も負荷が高い** |
| 上級 | 組み合わせが自動化されている |

上級者は積集合をリアルタイムで計算しているのではなく、
**「キリル文字 + EU 式ナンバー = ブルガリア」を 1 つの塊として記憶している。**
計算されるのではなく想起される。

**中級の壁は組み合わせの数である。** どれを覚えるべきか分からないまま
総当たりで蓄積するしかない。ここは計算で導出できる。

## 抽出条件

**件数の閾値ではなく、メタの区分で判定する。**
GeoGuessr の失点は距離で決まるため、同じ 3 カ国でも隣接なら失点が小さく、
大陸が違えば大きい。件数だけでは価値が決まらない。

| 条件 | 理由 |
|---|---|
| どちらも単独で 1 カ国に確定しない | 確定するなら組み合わせる意味がない。単独の方が軽い |
| 積集合が空でない | **空は両立しない観察である。** 絞り込みの成功ではない |
| 積集合が両方の単独より真に小さい | 同じなら組み合わせは何も足していない |
| 積集合が良メタ以上 | **3 カ国でも大陸をまたぐなら覚える価値が薄い** |

全 3,003 ペアから **48 件**を抽出した。

| 落とした理由 | 件数 |
|---|---|
| 片方が単独で 1 カ国に確定する | 657 |
| 積集合が空（両立しない） | 86 |
| 単独より縮まない | 1,337 |
| 掛けても補助メタのまま | 875 |

**計算で 48 件出たが、うち人手記述だけで成り立つのは 4 件である。**

| 由来 | 件数 | 使い方 |
|---|---|---|
| 人手記述のみ | **4** | 覚える対象にできる |
| AI 由来を含む | 44 | **断定に使わない。** 粗い用語（20 カ国以上）を掛けた結果である |

**そして `verified`（一次情報で確認済み）だけで成り立つ組み合わせは 0 件である。**

仕組みは動いている。**データが足りていない。**

理由は 2 つある。

1. `verified` の用語（ボラードや標識）は**単独で 1 カ国に確定するものが多い**。
   掛ける必要がないため抽出条件で落ちる
2. 組み合わせが強くなるのは**単独では弱いメタ同士**である。
   そして弱いメタは経験則（`heuristic`）として記録されているものが多い

**「組み合わせを計算で導出できる」ことと「導出した組み合わせが正しい」ことは別である。**
前者は集合演算で保証できるが、後者は辞書の質で決まる。

## 学習リスト（人手記述のみ）

**`確かさ` は弱い方に引かれる。** 片方が経験則なら組み合わせも経験則である。

| # | 組み合わせ | 単独の強さ | 積集合 | 確かさ |
|---|---|---|---|---|
| 1 | `road_marking` **中央線が黄色** ＋ `terrain_vegetation` **道路のすぐ横に木々** | 補助メタ 13 / 補助メタ 3 | **最強メタ（1 カ国 / 同一地域）** [BR] | heuristic |
| 2 | `road_marking` **中央線が黄色** ＋ `terrain_vegetation` **道路から離れて木々** | 補助メタ 13 / 補助メタ 2 | **最強メタ（1 カ国 / 同一地域）** [CA] | heuristic |
| 3 | `terrain_vegetation` **道路脇の石垣** ＋ `pole` **四角柱の電柱** | 補助メタ 3 / 補助メタ 2 | **最強メタ（1 カ国 / 同一地域）** [PE] | heuristic |
| 4 | `road_marking` **中央線が黄色** ＋ `terrain_vegetation` **道路脇の石垣** | 補助メタ 13 / 補助メタ 3 | **強メタ（2 カ国 / 同一地域）** [PE EC] | heuristic |

## 参考：AI 由来を含む組み合わせ

**これは学習リストではない。**

AI 由来の用語は「粗いこと」を条件に採っている（該当国 20 以上）。
役目は絞り込むことではなく、**「それでは絞り込めていない」と教えること**である。

それを人手記述と掛けると積集合は小さくなるが、
**縮んだ理由は AI 側の該当国リストが正しいという前提に乗っている。**
実測でモデル間の該当国は食い違う（黄色いボラードが 8 カ国 対 2 カ国）。

| # | 組み合わせ | 単独の強さ | 積集合 | 確かさ |
|---|---|---|---|---|
| 1 | `road_marking` **外側線が黄色** ＋ `season` **雪が地面に覆いかぶさっている** | 良メタ 4 / 補助メタ 39 | **最強メタ（1 カ国 / 同一地域）** [ZA] | unverified |
| 2 | `terrain_vegetation` **道路脇の石垣** ＋ `bollard` **反射板が赤色** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 3 | `terrain_vegetation` **道路脇の石垣** ＋ `pole` **T字型腕木** | 補助メタ 3 / 補助メタ 66 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 4 | `terrain_vegetation` **道路脇の石垣** ＋ `pole` **単一碍子** | 補助メタ 3 / 補助メタ 66 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 5 | `terrain_vegetation` **道路脇の石垣** ＋ `road_marking` **実線（solid line）** | 補助メタ 3 / 補助メタ 43 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 6 | `terrain_vegetation` **道路脇の石垣** ＋ `road_marking` **破線（dashed line）** | 補助メタ 3 / 補助メタ 43 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 7 | `terrain_vegetation` **道路脇の石垣** ＋ `road_marking` **白実線（white solid line）** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 8 | `terrain_vegetation` **道路脇の石垣** ＋ `road_marking` **白破線（white broken line）** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 9 | `terrain_vegetation` **道路脇の石垣** ＋ `season` **雪が地面に覆いかぶさっている** | 補助メタ 3 / 補助メタ 39 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 10 | `terrain_vegetation` **道路脇の石垣** ＋ `season` **落葉が地面に積もっている** | 補助メタ 3 / 補助メタ 31 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 11 | `terrain_vegetation` **道路脇の石垣** ＋ `season` **落葉が赤や黄色に色づいている** | 補助メタ 3 / 補助メタ 28 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 12 | `terrain_vegetation` **道路脇の石垣** ＋ `season` **道路に雪がある** | 補助メタ 3 / 補助メタ 26 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 13 | `terrain_vegetation` **道路脇の石垣** ＋ `vehicle` **EU帯プレート** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [GB] | unverified |
| 14 | `terrain_vegetation` **道路のすぐ横に木々** ＋ `bollard` **反射板が白色** | 補助メタ 3 / 補助メタ 41 | **最強メタ（1 カ国 / 同一地域）** [BR] | unverified |
| 15 | `terrain_vegetation` **道路のすぐ横に木々** ＋ `bollard` **円形断面のポール** | 補助メタ 3 / 補助メタ 40 | **最強メタ（1 カ国 / 同一地域）** [BR] | unverified |
| 16 | `terrain_vegetation` **道路のすぐ横に木々** ＋ `bollard` **反射板がオレンジ（アンバー）色** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [BR] | unverified |
| 17 | `terrain_vegetation` **道路のすぐ横に木々** ＋ `bollard` **反射板が赤色** | 補助メタ 3 / 補助メタ 38 | **最強メタ（1 カ国 / 同一地域）** [BR] | unverified |
| 18 | `terrain_vegetation` **道路のすぐ横に木々** ＋ `season` **影の方向が北を向いている（正午頃）** | 補助メタ 3 / 補助メタ 61 | **最強メタ（1 カ国 / 同一地域）** [BR] | unverified |
| 19 | `terrain_vegetation` **道路から離れて木々** ＋ `architecture` **スレート屋根** | 補助メタ 2 / 補助メタ 92 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 20 | `terrain_vegetation` **道路から離れて木々** ＋ `architecture` **金属屋根** | 補助メタ 2 / 補助メタ 91 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 21 | `terrain_vegetation` **道路から離れて木々** ＋ `architecture` **木屋根** | 補助メタ 2 / 補助メタ 91 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 22 | `terrain_vegetation` **道路から離れて木々** ＋ `architecture` **瓦屋根** | 補助メタ 2 / 補助メタ 79 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 23 | `terrain_vegetation` **道路から離れて木々** ＋ `pole` **コンクリート電柱** | 補助メタ 2 / 補助メタ 93 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 24 | `terrain_vegetation` **道路から離れて木々** ＋ `pole` **T字型腕木** | 補助メタ 2 / 補助メタ 66 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 25 | `terrain_vegetation` **道路から離れて木々** ＋ `pole` **単一碍子** | 補助メタ 2 / 補助メタ 66 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 26 | `terrain_vegetation` **道路から離れて木々** ＋ `road_marking` **白線（white line）** | 補助メタ 2 / 補助メタ 94 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 27 | `terrain_vegetation` **道路から離れて木々** ＋ `road_marking` **実線（solid line）** | 補助メタ 2 / 補助メタ 43 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 28 | `terrain_vegetation` **道路から離れて木々** ＋ `road_marking` **破線（dashed line）** | 補助メタ 2 / 補助メタ 43 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 29 | `terrain_vegetation` **道路から離れて木々** ＋ `script` **ラテン文字** | 補助メタ 2 / 補助メタ 79 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 30 | `terrain_vegetation` **道路から離れて木々** ＋ `script` **latin_script** | 補助メタ 2 / 補助メタ 66 | **最強メタ（1 カ国 / 同一地域）** [CA] | unverified |
| 31 | `terrain_vegetation` **道路から離れて木々** ＋ `traffic_side` **右側通行の国** | 補助メタ 2 / 補助メタ 65 | **最強メタ（1 カ国 / 同一地域）** [RU] | unverified |
| 32 | `pole` **四角柱の電柱** ＋ `script` **ラテン文字** | 補助メタ 2 / 補助メタ 79 | **最強メタ（1 カ国 / 同一地域）** [PE] | unverified |
| 33 | `pole` **四角柱の電柱** ＋ `season` **影の方向が北を向いている（正午頃）** | 補助メタ 2 / 補助メタ 61 | **最強メタ（1 カ国 / 同一地域）** [PE] | unverified |
| 34 | `pavement` **粗いアスファルト／砂埃が浮く黒路面** ＋ `script` **latin_accented** | 補助メタ 55 / 補助メタ 21 | **最強メタ（1 カ国 / 同一地域）** [TR] | unverified |
| 35 | `script` **latin_accented** ＋ `vehicle` **大型車主体** | 補助メタ 21 / 補助メタ 59 | **最強メタ（1 カ国 / 同一地域）** [TR] | unverified |
| 36 | `road_marking` **中央線が黄色** ＋ `season` **道路に雪がある** | 補助メタ 13 / 補助メタ 26 | **強メタ（2 カ国 / 同一地域）** [US CA] | unverified |
| 37 | `terrain_vegetation` **道路脇の石垣** ＋ `architecture` **瓦屋根** | 補助メタ 3 / 補助メタ 79 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 38 | `terrain_vegetation` **道路脇の石垣** ＋ `pavement` **粗いアスファルト／砂埃が浮く黒路面** | 補助メタ 3 / 補助メタ 55 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 39 | `terrain_vegetation` **道路脇の石垣** ＋ `pole` **コンクリート電柱** | 補助メタ 3 / 補助メタ 93 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 40 | `terrain_vegetation` **道路脇の石垣** ＋ `traffic_side` **右側通行の国（アジア）** | 補助メタ 3 / 補助メタ 100 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 41 | `terrain_vegetation` **道路脇の石垣** ＋ `traffic_side` **右側通行（ほぼ決定的）** | 補助メタ 3 / 補助メタ 96 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 42 | `terrain_vegetation` **道路脇の石垣** ＋ `traffic_side` **右側通行（粗い）** | 補助メタ 3 / 補助メタ 92 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 43 | `terrain_vegetation` **道路脇の石垣** ＋ `vehicle` **白背景・黒文字** | 補助メタ 3 / 補助メタ 100 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |
| 44 | `terrain_vegetation` **道路脇の石垣** ＋ `vehicle` **大型車主体** | 補助メタ 3 / 補助メタ 59 | **強メタ（2 カ国 / 同一地域）** [PE EC] | unverified |

## 読み方

- **弱いメタ同士の組み合わせが強くなる例を探すための表である。**
  単独では 3 カ国でも、掛けて 1 カ国になるならそれが覚える単位になる
- **上から順に覚える必要はない。** 自分がプレイする地域に出るものから覚える
- 積集合が空になったペアは表に出していない。**それは矛盾であり絞り込みではない**
- **人手記述が 15 語しかないため、組み合わせの数もそこで決まる。** 辞書を増やすほど表は伸びる
