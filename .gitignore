node_modules
.nuxt
.output
dist

# 認証情報。絶対にコミットしない
.env
.env.local
.env.*.local

# 画像は 1 枚も置かない（Street View の画像はキャッシュ・再配布が禁止されている）
# svg も除外する。**「全除外している」と書くなら、本当に全部を除外する**
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
*.tiff
*.svg
*.avif
*.ico

# 一時ファイル
.scratch/
*.log

# データセット取り込みの控え。**取り消せる操作にするために作る**
.backup/

# 利用者ごとの状態。**索引はディレクトリから復元できるのでコミットしない**
data/library.json
data/progress.json
